% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitBSFA.R
\name{fitBSFA}
\alias{fitBSFA}
\title{Fits a Bayesian Sparse Factor models under either the UGLT or PLT constraint.}
\usage{
fitBSFA(
  y,
  constraint,
  fixed,
  q,
  n_runs,
  alpha,
  beta,
  theta.shape,
  theta.rate,
  hyperparams,
  thin = 1,
  burn = 1
)
}
\arguments{
\item{y}{\eqn{v \times N} data matrix \eqn{Y}.}

\item{constraint}{Either 'UGLT' or 'PLT'.}

\item{fixed}{Either TRUE or FALSE. Determines whether to return estimates with spurious factors removed.}

\item{q}{Number of factors \eqn{q}.}

\item{n_runs}{Total number of MCMC iterations.}

\item{alpha}{Length-v shape parameters \eqn{\alpha} for the \eqn{G^{-1}} prior on \eqn{\sigma^2}.}

\item{beta}{Length-v rate parameters \eqn{\beta} for the \eqn{G^{-1}} prior on \eqn{\sigma^2}.}

\item{theta.shape}{Scalar shape hyperparameter \eqn{a_\theta} for the \eqn{G^{-1}} prior on \eqn{\theta}.}

\item{theta.rate}{Scalar rate hyperparameter \eqn{b_\theta} for the \eqn{G^{-1}} prior on \eqn{\theta}.}

\item{hyperparams}{List with \code{aH} and \code{bH} for the Beta prior on \eqn{\tau}.}

\item{thin}{Thinning interval (retain every \code{thin}-th draw after burn-in).}

\item{burn}{Index of the first draw to retain.}
}
\value{
List with \code{estimates} (posterior summaries by factor dimension r)
and \code{draws} (tibble of retained MCMC draws).
}
\description{
Implements a partially collapsed gibbs sampler. Each iteration Each iteration samples \eqn{\tau}, \eqn{\delta},
\eqn{l}, \eqn{\Lambda}, \eqn{\sigma^2}, \eqn{F}, and \eqn{\theta} in sequence, followed by
a boosting step step. Applies post-processing: spurious factor removal, sign fixing, orders columns, and filters out draws that
fail the 3579 counting rule and filters out unidentified draws.
}
