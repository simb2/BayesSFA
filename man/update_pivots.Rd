% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_pivots.R
\name{update_pivots}
\alias{update_pivots}
\title{Update pivot rows via Metropolis-Hastings moves}
\usage{
update_pivots(
  delta,
  pivots,
  factors,
  y,
  theta,
  alpha,
  beta,
  hyperparams,
  move_probs = list(pshift = 0.3, pswitch = 0.3, pa = 0.5)
)
}
\arguments{
\item{delta}{\eqn{v \times q} binary sparsity matrix \eqn{\delta}.}

\item{pivots}{Length-q vector of current pivot row indices \eqn{l_1, \ldots, l_q}.}

\item{factors}{\eqn{q \times N} factor matrix \eqn{F}.}

\item{y}{\eqn{v \times N} data matrix \eqn{Y}.}

\item{theta}{Length-q column shrinkage vector \eqn{\theta}.}

\item{alpha}{Length-v shape parameters \eqn{\alpha} for the \eqn{G^{-1}} prior on \eqn{\sigma^2}.}

\item{beta}{Length-v rate parameters \eqn{\beta} for the \eqn{G^{-1}} prior on \eqn{\sigma^2}.}

\item{hyperparams}{List with elements \code{aH} and \code{bH} for the Beta prior on \eqn{\tau_j}.}

\item{move_probs}{List with elements \code{pshift}, \code{pswitch}, \code{pa} controlling
the probability of each move type.}
}
\value{
List with updated \code{delta} (\eqn{v \times q}) and \code{pivots} (length-q).
}
\description{
Applies one randomly chosen move per column j (shift, switch, or add/delete)
to update the pivot vector \eqn{l = (l_1, \ldots, l_q)} and the sparsity matrix \eqn{\delta}.
Columns are visited in random order.
}
