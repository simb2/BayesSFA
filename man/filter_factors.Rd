% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_factors.R
\name{filter_factors}
\alias{filter_factors}
\title{Remove spurious factors from posterior draws}
\usage{
filter_factors(
  pivot_draws,
  factor_draws,
  lambda_draws,
  delta_draws,
  theta_draws,
  sigma_draws,
  tau_draws,
  n_draws
)
}
\arguments{
\item{pivot_draws}{List of length-q pivot vectors, one per draw.}

\item{factor_draws}{List of \eqn{q \times N} factor matrices, one per draw.}

\item{lambda_draws}{List of \eqn{v \times q} loading matrices, one per draw.}

\item{delta_draws}{List of \eqn{v \times q} sparsity matrices, one per draw.}

\item{theta_draws}{List of length-q \eqn{\theta} vectors, one per draw.}

\item{sigma_draws}{List of length-v \eqn{\sigma^2} vectors, one per draw.}

\item{tau_draws}{List of length-q \eqn{\tau} vectors, one per draw.}

\item{n_draws}{Number of MCMC draws.}
}
\value{
List with the same fields, spurious columns removed from each draw.
}
\description{
A factor column j is spurious when only its pivot row is active
(\eqn{\sum_k \delta_{kj} = 1}). Removes each spurious column from \eqn{\Lambda}, \eqn{F},
\eqn{\delta}, \eqn{\theta}, and \eqn{\tau} across all draws, and recomputes pivot indices accordingly.
}
