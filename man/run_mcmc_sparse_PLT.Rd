% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_mcmc_sparse_PLT_Static.R
\name{run_mcmc_sparse_PLT}
\alias{run_mcmc_sparse_PLT}
\title{Run the sparse PLT factor model MCMC sampler (static pivots)}
\usage{
run_mcmc_sparse_PLT(
  N,
  q,
  n_runs,
  alpha,
  beta,
  theta.shape,
  theta.rate,
  hyperparams,
  data,
  thin = 1,
  burn = 1,
  ident = TRUE
)
}
\arguments{
\item{N}{Number of observations N (should equal \code{ncol(data)}).}

\item{q}{Number of factors \eqn{q}.}

\item{n_runs}{Total number of MCMC iterations.}

\item{alpha}{Length-v shape parameters \eqn{\alpha} for the \eqn{G^{-1}} prior on \eqn{\sigma^2}.}

\item{beta}{Length-v rate parameters \eqn{\beta} for the \eqn{G^{-1}} prior on \eqn{\sigma^2}.}

\item{theta.shape}{Scalar shape hyperparameter \eqn{a_\theta} for the \eqn{G^{-1}} prior on \eqn{\theta}.}

\item{theta.rate}{Scalar rate hyperparameter \eqn{b_\theta} for the \eqn{G^{-1}} prior on \eqn{\theta}.}

\item{hyperparams}{List with \code{aH} and \code{bH} for the Beta prior on \eqn{\tau}.}

\item{data}{\eqn{v \times N} data matrix \eqn{Y}.}

\item{thin}{Thinning interval (retain every \code{thin}-th draw after burn-in).}

\item{burn}{Index of the first draw to retain.}

\item{ident}{Logical; if \code{TRUE}, discard draws that fail the 3579 counting rule.}
}
\value{
List with \code{estimates} (posterior summaries by factor dimension r)
and \code{draws} (tibble of retained MCMC draws).
}
\description{
Implements a Gibbs sampler for the sparse PLT model with fixed pivots
\eqn{l_j = j}. Each iteration samples \eqn{\tau}, \eqn{\delta}, \eqn{\Lambda}, \eqn{\sigma^2},
\eqn{F}, and \eqn{\theta} in sequence, followed by the GIG boost step.
Applies post-processing: identifiability filtering, column reordering by
pivot, and sign fixing
}
