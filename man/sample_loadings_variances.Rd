% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_loadings_delta.R
\name{sample_loadings_variances}
\alias{sample_loadings_variances}
\title{Sample loadings and idiosyncratic variances (UGLT model)}
\usage{
sample_loadings_variances(
  data,
  Factors,
  delta,
  theta,
  alpha,
  beta,
  inner_prod_y
)
}
\arguments{
\item{data}{\eqn{v \times N} data matrix \eqn{Y}.}

\item{Factors}{\eqn{q \times N} factor matrix \eqn{F}.}

\item{delta}{\eqn{v \times q} binary sparsity matrix \eqn{\delta}.}

\item{theta}{Length-q column shrinkage vector \eqn{\theta}.}

\item{alpha}{Length-v shape parameters \eqn{\alpha_1, \ldots, \alpha_v} for the \eqn{G^{-1}} prior on \eqn{\sigma^2}.}

\item{beta}{Length-v rate parameters \eqn{\beta_1, \ldots, \beta_v} for the \eqn{G^{-1}} prior on \eqn{\sigma^2}.}

\item{inner_prod_y}{Length-v vector of precomputed row inner products \eqn{\sum_t y_{kt}^2}.}
}
\value{
List with \code{Lambda_new} (\eqn{v \times q} matrix) and \code{sigma2_new} (length-v vector).
}
\description{
Draws \eqn{\lambda_{k,\delta_k}} and \eqn{\sigma^2_k} jointly from their Normal-Inverse-Gamma conditional
posteriors for each row k. Entries with \eqn{\delta_{kj} = 0} remain zero.
Prior: \eqn{\lambda_{kj} \mid \delta_{kj}=1 \sim N(0, \theta_j)} and \eqn{\sigma^2_k \sim G^{-1}(\alpha_k, \beta_k)}.
}
